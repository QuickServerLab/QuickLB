auto_start=true
flag tells if the interface should start on load of LB

distribution=ip_based
Following options are available roundrobin, failover, random, ip_based

monitoringIntervalInSec=60
Monitoring Interval In Sec for all the hosts under this interface
